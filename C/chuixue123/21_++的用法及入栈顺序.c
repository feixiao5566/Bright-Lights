#include<stdio.h>

void main()
{
    int a = 0, b = 0;
    printf("%d, %d, %d\n", a++, ++b, a+b);
}

/*这道题主要考的是printf函数的入栈顺序
 * 可变参数的函数,入栈顺序是 从右往左
 * 因此,是先运行a+b, 即先算第三个值为0
 * 然后++b,值为1
 * 然后a++, 先赋值再加,值0
 * 0, 1, 0
 * */
